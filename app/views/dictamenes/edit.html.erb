<% content_for :titulo_de_pagina, "Editando Dictamen #{@dictamen.identificador}" %>

<div class="botonera row">
  <div class="navegacion col d-flex justify-content-end">
    <%= link_to 'Volver', expediente_path(@expediente), class: 'btn btn-dark' %>
  </div>
</div>

<div class="contenido row">
  <div class="col">
    <%= render 'form', expediente: @expediente, dictamen: @dictamen %>
  </div>
</div>
